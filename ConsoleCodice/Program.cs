using System;
using System.IO;

namespace ConsoleCodice
{
    class Program
    {
        static void Main(string[] args)
        {
            string file = "input.txt";
            using(StreamReader sr=new StreamReader(file))
            {
                string num = sr.ReadLine();
                int x=
            }
        }
    }
}
